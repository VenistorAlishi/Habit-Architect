# Habit Architect — Study Sprints

**Habit Architect** — это небольшое веб-приложение для студентов, которое помогает планировать учебные спринты, выстраивать привычки и отслеживать прогресс по фокус-сессиям.

Проект сделан в рамках хакатона в треке **Self-Development & Learning**.

---

## Задача продукта

Цель приложения — не просто «тудушка», а **структура для осознанной подготовки**:

- Разбить подготовку на **спринты** (например, неделя перед экзаменом).
- Внутри спринта задать **привычки**: «решать 20 задач», «читать конспект 30 минут».
- Запланировать конкретные **фокус-сессии** с датой, временем и длительностью.
- Отмечать сессии как **выполненные** или **пропущенные**, фиксировать сложность и настроение.
- Смотреть простую **статистику по спринту**: сколько сессий запланировано, сколько сделано, какой процент выполнения и сколько минут реально отработано.

---

## Основные возможности

### 🎯 Спринты

- Создание спринтов с полями:
  - `title` — название;
  - `goal_text` — цель;
  - `start_date`, `end_date` — даты начала и окончания;
  - `status` — `planned / active / completed / cancelled`.
- Список всех спринтов с краткой информацией.
- Детальная страница спринта с прогрессом.

### 📌 Привычки внутри спринта

- Привязаны к конкретному спринту (`sprint_id`).
- Поля:
  - `name` — название привычки;
  - `description` — необязательное описание;
  - `target_sessions_per_day` — целевое число сессий в день.
- В UI:
  - список привычек на странице спринта;
  - форма **создания привычки**;
  - кнопка **удаления привычки** (опционально с удалением связанных сессий).

### ⏱ Фокус-сессии

- Привязаны к спринту (обязательно) и опционально к привычке.
- Поля:
  - `planned_start` — планируемое время старта;
  - `planned_duration_min` — планируемая длительность (минуты);
  - `actual_start`, `actual_duration_min` — фактические значения;
  - `status` — `planned / done / skipped`;
  - `notes` — заметки;
  - `difficulty` (1–5);
  - `mood` (1–5).
- В UI:
  - таблица сессий на странице спринта;
  - форма **планирования** сессии (дата, время, длительность, привычка, заметка);
  - кнопки **Mark as done** и **Skip**;
  - фильтры по статусу: `All / Planned / Done / Skipped`.

### 📊 Статистика спринта

На странице спринта отображается агрегированная статистика:

- количество привычек;
- количество всех сессий;
- количество выполненных сессий;
- процент выполнения (`completion_rate`);
- суммарные планируемые минуты;
- суммарные фактические минуты (если есть выполненные сессии).

---

## Технологический стек

- **Backend**: [FastAPI](https://fastapi.tiangolo.com/)
- **ORM**: SQLAlchemy
- **Валидация и схемы**: Pydantic v2
- **База данных**: SQLite (файл, создаётся автоматически)
- **Шаблоны**: Jinja2 (HTML-шаблоны в `app/templates`)
- **Статика / стили**: CSS в `app/static/style.css`
- **Тестирование**: pytest
- **Запуск**: `uvicorn` (можно через `uv` или обычный `pip + venv`)

---

## Быстрый старт (локально)

### 1. Требования

- Python 3.11+ (желательно)
- Git / zip с проектом

### 2. Клонирование/распаковка

```bash
git clone <url-репозитория> habit-architect
cd habit-architect
```

либо просто зайти в директорию проекта, если он уже скачан.

### 3. Установка зависимостей

#### Вариант A: через обычный venv + pip

```bash
python -m venv .venv
# PowerShell:
.\.venv\Scripts\Activate.ps1
# CMD:
.\.venv\Scriptsctivate.bat

python -m pip install --upgrade pip
pip install -r requirements.txt
```

Если `requirements.txt` нет, минимум:

```bash
pip install fastapi "uvicorn[standard]" sqlalchemy jinja2 python-multipart pytest
```

#### Вариант B: через uv

Если установлен [`uv`](https://github.com/astral-sh/uv):

```bash
uv sync
```

---

### 4. Инициализация базы данных

В проекте может быть helper для создания таблиц, например в `app/db.py`. Тогда:

```bash
python -m app.db
```

или, если используется uv:

```bash
uv run python -m app.db
```

(либо вызвать `init_db()` из `app.db`, если такая функция есть.)

---

### 5. Запуск сервера разработки

```bash
uvicorn app.main:app --reload
```

или:

```bash
uv run uvicorn app.main:app --reload
```

После запуска:

- UI со списком спринтов:  
  `http://127.0.0.1:8000/ui/`
- Swagger (JSON API docs):  
  `http://127.0.0.1:8000/docs`

---

## Маршруты

### UI (HTML)

- `GET /ui/`  
  Список спринтов + форма создания спринта.

- `POST /ui/sprints`  
  Создание спринта из формы.

- `GET /ui/sprints/{sprint_id}`  
  Детальная страница спринта:  
  - карточка спринта и статистика,  
  - список привычек (с формой добавления и кнопкой удаления),  
  - список сессий, фильтры по статусу, форма планирования сессий.

- `POST /ui/sprints/{sprint_id}/habits`  
  Создание привычки в спринте.

- `POST /ui/habits/{habit_id}/delete`  
  Удаление привычки (и, при необходимости, связанных сессий).

- `POST /ui/sprints/{sprint_id}/sessions/plan`  
  Планирование новой сессии.

- `POST /ui/sessions/{session_id}/complete`  
  Отметить сессию как выполненную.

- `POST /ui/sessions/{session_id}/skip`  
  Отметить сессию как пропущенную.

Все эти маршруты возвращают HTML и не попадают в OpenAPI.

### JSON API (кратко)

Точные схемы завязаны на Pydantic-моделях в `app/schemas`, но в целом есть:

- `GET /sprints` — список спринтов.
- `POST /sprints` — создание спринта.
- `GET /sprints/{id}` — детали спринта.
- `PATCH /sprints/{id}/status` — смена статуса спринта.
- `GET /sprints/overview` — агрегированная статистика по спринтам.
- `POST /sprints/{id}/habits` — создание привычки в спринте.
- `GET /sprints/{id}/habits` — список привычек спринта.
- `POST /sessions` / `GET /sessions` / `PATCH /sessions/{id}/complete` / `PATCH /sessions/{id}/skip` — работа с сессиями.

(конкретные пути можно посмотреть в `app/routes/sprints.py` и Swagger `/docs`.)

---

## Структура проекта

Примерная структура:

```text
app/
  main.py               # создание FastAPI-приложения, подключение роутеров и шаблонов
  db.py                 # настройка БД, сессии, init_db
  models.py             # SQLAlchemy-модели Sprint, Habit, Session
  schemas/
    __init__.py         # Pydantic-схемы для API и UI
  routes/
    sprints.py          # JSON API для спринтов/привычек/сессий
    ui.py               # HTML UI под /ui
    demo.py             # демо/seed-роуты (если используются)
  templates/
    base.html           # базовый шаблон
    sprints_list.html   # список спринтов
    sprint_detail.html  # детали спринта, привычки, сессии, формы
  static/
    style.css           # стили приложения

tests/
  test_sprints_api.py   # тесты API по спринтам
  test_demo_api.py      # тесты демо/seed
  conftest.py           # фикстуры (например, TestClient, БД)
```

---

## Тестирование

Запуск тестов:

```bash
pytest
```

или:

```bash
uv run pytest
```

Все существующие тесты должны проходить без модификаций — это базовый критерий корректности изменений.

---

## Идеи для развития

Если проект будет развиваться дальше, можно добавить:

- Авторизацию / многопользовательский режим (собственные спринты для каждого пользователя).
- Более продвинутую аналитику:
  - графики выполненных сессий по дням;
  - связь настроения/сложности с результатами.
- Экспорт/импорт спринтов (например, в JSON или Markdown).
- Мобильную адаптацию UI и быстрые действия (hotkeys) для отметки сессий.
- Интеграции с календарём или Telegram-ботом для напоминаний.

---

## Лицензия

Добавьте сюда текст лицензии, если вы её выбираете (например, MIT, Apache 2.0 и т.д.).
